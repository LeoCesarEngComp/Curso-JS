/*
function soma(n1, n2) {
    let sum = n1 + n2
    return `A soma de ${n1} e ${n2} é ${sum}!`
}


console.log(soma(10, 20))
*/

/*
function soma(n1=0, n2=0) {//os valores já foram pre-definidos, caso não seja lançado um dos parâmetros
    let sum = n1 + n2
    return `A soma de ${n1} e ${n2} é ${sum}!`
}

console.log(soma(2))
*/

let double = function doub(x) {
    return x * 2
}

console.log(double(5))